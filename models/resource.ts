import { HTMLSource } from "react-native-render-html";

export interface Resource {
    id: number;
    name: string;
    author_name: string;
    slug: string;
    language_name: string;
    content: string;
}